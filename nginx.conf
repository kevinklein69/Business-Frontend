server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # trailingSlash: true + output: export → Routen liegen als <pfad>/index.html.
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }
}
